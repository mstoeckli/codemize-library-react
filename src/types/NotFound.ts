import { DefaultTheme } from "styled-components";

/** @public
 *  @desc Defines the properties of each not found element */
export interface INotFoundInProps {
    theme?: DefaultTheme
}

/** @public
 *  @desc Defines the theme properties for all the not found config */
export type NotFoundConfigTheme = {

}